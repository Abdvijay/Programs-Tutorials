#include<stdio.h>
void main(){
    int count,sum=0,i=1;
    printf("Enter Number : ");
    scanf("%d",&count);
    int temp = count;
    while(i <= count){
        sum = sum + i;
        i++;
    }
    printf("Sum Of First %d Numbers = %d",temp,sum);
}